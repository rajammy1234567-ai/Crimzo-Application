/** Voice/video call earnings: receiver share vs platform (owner) share */
const CALL_RECEIVER_SHARE = 0.7;
const CALL_PLATFORM_SHARE = 0.3;

module.exports = {
  CALL_RECEIVER_SHARE,
  CALL_PLATFORM_SHARE,
};